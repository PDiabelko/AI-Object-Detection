import streamlit as st

def main():
    st.set_page_config(
        page_title="Crosswalk Detection App",
        page_icon="🚸",
        layout="wide"
    )
    st.title("Crosswalk Detection App")
    

if __name__ == "__main__":
    main()